# バックエンドエンジニア 作業ログ

## 2026-06-12
- 作業指示書を受領。Express + better-sqlite3 + TypeScript で構成。
- 実装：
  - `src/db.ts`：SQLite初期化、スキーマ（companies / events）、初回サンプルデータ投入。
  - `src/index.ts`：企業CRUD・イベントCRUD・ダッシュボード・ヘルスチェック。
  - バリデーション（必須・status enum・日付形式）と異常系（400/404/500）。
  - CORSは localhost:5173 のみ許可。DBはローカル `data/` に保存（外部送信なし）。
- ドキュメント：API仕様書.md を作成。
- PMへ完了報告。レビュー指摘（N+1クエリ＝低）を認識、現規模では許容。今後JOIN化を検討。
- 注意：better-sqlite3 はネイティブビルドが必要。Node 18+ 推奨。

## 2026-06-12（第3サイクル）
- `GET /api/events`（企業名付き）追加。tasks テーブル＋CRUD、export/import API を実装。詳細に tasks 同梱。
- `tsc --noEmit` エラー0、curl で各エンドポイント 200 確認。API仕様書を更新。

## 2026-06-13（第8サイクル／Codex実装・Claudeレビュー）
- `es_templates` テーブル追加（`CREATE TABLE IF NOT EXISTS`、マイグレーション不要）。
- ESテンプレCRUD：`GET/POST/PUT/DELETE /api/es-templates`（タイトル必須・カテゴリenum検証・404）。
- `GET /api/stats`：ステータス別／業界別（NULL・空は「未設定」）／内定・お祈り・進行中をSQL側で集計。
- `export` を `version: 2` 化し `esTemplates` を同梱、`import` は `esTemplates` 省略可で後方互換。
- 検証：`tsc --noEmit` 0、スモーク（stats・CRUD・export・不正カテゴリ400）OK。API仕様書を追記更新。
- 注意：カテゴリenumが確定指示と相違（`その他`→`逆質問`）。ユーザー判断で逆質問版を採用、`types.ts` と一致。
