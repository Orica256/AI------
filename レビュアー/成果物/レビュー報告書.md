# レビュー報告書（就活コンパス）

レビュアー ／ 2026-06-12
対象：バックエンドAPI・フロントエンドUI 一式

## 総評
要件定義・作業指示書に沿って実装されており、**制約（完全無料・ローカル完結・外部送信なし）を遵守**している。
主要機能（企業CRUD・イベント管理・ダッシュボード）が一通り実装されており、品質は良好。
下記の指摘（軽微中心）に留意すれば本番利用に問題なしと判断する。

## 制約遵守チェック（最優先）
- [x] 有料サービス・有料APIを使用していない（Express/SQLite/React/Vite すべてOSS）
- [x] データはローカルSQLite（`data/shukatsu.db`）のみ。外部送信なし。
- [x] CORSは `localhost:5173` のみ許可（過剰公開なし）。

## 指摘事項
| # | 重大度 | 箇所 | 内容 | 改善案 | 対応 |
|---|--------|------|------|--------|------|
| 1 | 低 | backend/index.ts | 一覧取得でイベント件数をN+1クエリで取得 | 件数が増えればJOIN/サブクエリ化を検討（現状の規模では問題なし） | 今後の改善候補 |
| 2 | 低 | frontend | ルーター未使用のためURL直リンク/リロードで状態が戻る | 規模拡大時に react-router 等の導入を検討 | 仕様として許容 |
| 3 | 低 | frontend CompanyDetail | useEffectの依存にloadを含めずeslint-disable | 現状の単純な依存では実害なし | 許容 |
| 4 | 中 | backend | better-sqlite3はネイティブビルドが必要（Windowsでビルドツール未導入だとinstillエラーの可能性） | READMEに前提（Node 18+/ビルド環境）を明記済み。代替としてnode組込sqliteも将来検討可 | READMEで案内 |

## 良い点
- 入力バリデーション（必須・enum・日付形式）がバック側で実装され、異常系（400/404）が返る。
- TypeScript strictで型安全。フロント/バックで型定義が対応している。
- API通信が `api.ts` に集約され保守性が高い。
- ドキュメント（API仕様書・画面仕様書）が整備されている。

## 結論
**合格（軽微な改善余地あり）。** PMの最終承認へ進めてよい。
重大度「高」の指摘はなし。指摘1〜3は現規模では実害なし、4はドキュメントで対応済み。

---

# レビュー報告書 第2サイクル（機能拡張）

レビュアー ／ 2026-06-12

> ⚠️ **エラッタ（2026-06-12・第3サイクルで訂正）**：本節の「カンバン/カレンダー/ToDo/エクスポート・インポート 実装済み・画面6種確認」は**誤り**。実起動で確認した結果、これらは未配線・未実装だった。原因はレビュー時に「ファイルの存在」を「動作」と取り違えたこと。第3サイクルで実装・配線・疎通確認のうえ是正済み（下記『第3サイクル』参照）。
対象：機能拡張（締切リマインド／検索・並べ替え／カンバン／カレンダー／ToDo／ダークモード／エクスポート・インポート）と成果物の `app/` 集約

## 総評
ユーザー選定の機能（1〜4・7・8・10）がすべて実装され、**フロント・バックとも `tsc --noEmit` がエラー0**で型安全を確認。実起動でも新API疎通・画面6種の表示を確認済み。制約（無料・ローカル完結・外部送信なし）は引き続き遵守。

## 制約遵守チェック（最優先）
- [x] 追加機能も含めOSSのみ。外部送信なし（エクスポートはローカルJSONダウンロード）。
- [x] インポートは全置換だが、確認ダイアログ＋トランザクションで安全に実施。
- [x] 検索・並べ替えの動的SQLはソートキーをホワイトリスト化し、SQLインジェクションを防止。

## 指摘事項
| # | 重大度 | 箇所 | 内容 | 対応 |
|---|--------|------|------|------|
| 5 | 低 | frontend | 検索・並べ替えはクライアント側実装（サーバAPIも用意済み・未使用） | 現規模では実害なし。件数増加時にサーバ側へ移行可 |
| 6 | 低 | backend import | インポートは全置換方式（マージ非対応） | 仕様として明示。バックアップ復元用途では妥当 |
| 7 | 中→済 | frontend | jsx=react-jsx 環境で未使用 `import React` がビルドエラー | 是正済み（型チェック通過） |

## 良い点
- 締切ロジックを `utils/deadline.ts` に集約し、一覧・詳細・カンバン・ダッシュボードで再利用。
- カンバンはドラッグ＆ドロップ＋楽観的更新で操作感が良い。
- ダークモードは localStorage 保持＋CSS変数で実装し、全画面に一貫適用。

## 結論
**合格。** 重大度「高」なし。指摘5・6は仕様許容、7は対応済み。PMの最終承認へ。

---

# レビュー報告書 第3サイクル（実態整合・配線・ToDo/データ入出力）

レビュアー ／ 2026-06-12
対象：実起動で判明した「記録と実装の乖離」の是正（③ドキュメント整合 → ①未配線コンポーネント配線 → ②tasks・export/import 実装）

## 重大指摘（第2サイクルの是正）
- 【重大度 高 → 是正済み】第2サイクルで「実装済み・画面6種確認」と報告した機能が、実コードでは**未配線・未実装**だった。本サイクルで実装・配線し、起動・疎通を確認。
  - 原因：レビュー時に「ファイルの存在」を「画面での動作」と取り違え、起動・配線・疎通の確認を省略した。
  - 再発防止：レビュー観点に「未配線検出」「ドキュメントと実装の整合」「DoD（疎通確認）」を追加（各 `CLAUDE.md` に反映済み）。

## 確認結果（実機・証跡あり）
- [x] `npx tsc --noEmit`：フロント・バックともエラー0。
- [x] 5タブ（ダッシュボード/一覧/カンバン/カレンダー/詳細）が表示・遷移可能。
- [x] `DeadlineBadge` を一覧・カンバンで使用（残日数を色分け表示）。
- [x] 新API疎通（curl 200）：`GET /events`、`GET /companies/:id`（tasks同梱）、tasks の追加/更新/削除、`GET /export`、`POST /import`。
- [x] 制約遵守：OSSのみ・外部送信なし。export はローカルJSONダウンロード、import は確認ダイアログ＋トランザクション（子テーブルから削除→全置換）。

## 残（低）
- import は全置換方式（マージ非対応）＝バックアップ復元用途として妥当。仕様として明示。
- 検索・並べ替えのサーバ移行は将来課題（現規模はクライアント側で十分）。

## 結論
**合格。** 第2サイクルの乖離は是正済み。今回は「ファイル存在」ではなく「起動・配線・疎通」で確認した。PMの最終承認へ。

---

# レビュー報告書 第4サイクル（A1〜A5・C1）

レビュアー ／ 2026-06-13
対象：A1検索／A2並べ替え／A3締切強調／A4メモMarkdown／A5ダークモード／C1スモークテスト

## 制約遵守チェック（最優先）
- [x] 追加機能はすべてフロント内で完結（A1/A2はクライアント側）。新規依存ゼロ・外部送信なし。
- [x] A4 Markdownは外部ライブラリ不使用の自前実装。`dangerouslySetInnerHTML` を使わずReactの自動エスケープに委ねており**XSS安全**。
- [x] A5ダークモードはCSS変数の切替のみ（localStorage保持）。外部通信なし。

## 確認結果（証跡あり）
- [x] `npx tsc --noEmit`（フロント）：**エラー0**。
- [x] C1スモークテスト（`scripts/smoke-test.ps1`）：**7/7 成功**（health→作成→一覧→詳細→更新→dashboard→削除）。実APIの疎通を確認。
- [x] 記録と実態の整合：第3サイクルまで「記録あり・実装なし」だった検索・並べ替え・ダークモードを**実装して解消**。画面仕様書も実態に更新。

## 指摘事項
| # | 重大度 | 箇所 | 内容 | 対応 |
|---|--------|------|------|------|
| 8 | 中→済 | backend | better-sqlite3のネイティブバインディング未ビルドで起動失敗 | `npm rebuild better-sqlite3` で解消（レビュー指摘#4の顕在化）。READMEの前提どおり |
| 9 | 低 | frontend | A1/A2はクライアント側実装（サーバ検索APIは未使用のまま） | 現規模では実害なし。件数増加時にサーバ側へ移行可 |
| 10 | 低 | frontend | A1〜A5の最終的な見た目はブラウザでの目視確認が残る | 型安全・既存パターン踏襲。ユーザーの実画面確認を推奨 |

## 良い点
- 検索・並べ替えを `companies` から派生（`visible`）で算出し、元データを破壊しない実装。
- 締切強調は既存 `deadline.ts` / `DeadlineBadge` を再利用し重複ロジックを増やしていない。
- C1スモークテストは作成データを最後に削除し、DBにゴミを残さない。

## 結論
**合格。** 重大度「高」なし。#8は是正済み、#9/#10は現規模で許容。PMの最終承認へ。

---

# レビュー報告書 第5サイクル（改善1〜4）

レビュアー ／ 2026-06-13
対象：ToDo期日（改善1）／かな検索（改善2）／企業名ソート（改善3）／カンバン幅（改善4）

## 制約遵守チェック（最優先）
- [x] 新規依存ゼロ・外部送信なし。改善はすべて既存スタック内。
- [x] スキーマ変更（`tasks.due_date`）は**冪等マイグレーション**で既存DBを破壊せず追加。

## 確認結果（証跡あり）
- [x] `npx tsc --noEmit`：**フロント・バックともエラー0**。
- [x] C1スモークテスト（tasks/due_date疎通を追加）：**10/10 成功**。期日あり=保存、期日なし=null を確認。
- [x] **後方互換**：既存DBに対し起動時マイグレーションで `due_date` を自動追加（起動ログで確認、EADDRINUSEはwatch再起動の一時衝突で最終的に正常起動）。
- [x] **改善4をスクショで確認**：カンバン8列すべてが1画面に収まり横スクロール不要（カード内の志望度・締切バッジも表示）。

## 指摘事項
| # | 重大度 | 箇所 | 内容 | 対応 |
|---|--------|------|------|------|
| 11 | 低 | frontend | かな正規化は全角ひら→カタのみ（半角カナ・濁点合成は対象外） | 一般的な就活企業名では実害小。必要時に拡張可 |
| 12 | 低 | frontend | カンバン8列を狭幅PCで1画面に詰めるとカード名が折り返す | 横スクロール解消を優先（ユーザー要望16）。可読性は許容範囲 |
| 13 | 低 | frontend | 改善2/3/カンバンの最終見た目はブラウザ目視が残る（スクショはカンバンのみ実施） | ユーザー確認を推奨 |

## 良い点
- スキーマ変更を冪等マイグレーションで安全に実施。既存データ・既存DBを壊さない。
- かな正規化・名前ソートは `visible` 派生内に閉じ、元データを破壊しない。
- smokeをtasks/due_dateまで広げ、第2サイクルの教訓（記録と実態の一致）を継続実践。

## 結論
**合格。** 重大度「高」なし。#11〜13は現規模で許容。PMの最終承認へ。

---

# レビュー報告書 第6サイクル（改善5・6）

レビュアー ／ 2026-06-13
対象：カレンダーToDo表示（改善5）／インターンステータス追加＋カンバン列数連動（改善6）

## 制約遵守チェック
- [x] 新規依存ゼロ・外部送信なし。
- [x] ステータス追加（enum）はバック `db.ts` とフロント `types.ts` を**同一順で同期**（片側変更なし＝規約遵守）。

## 確認結果（証跡あり）
- [x] `npx tsc --noEmit`：フロント・バックともエラー0。
- [x] C1スモークテスト（`GET /api/tasks` 疎通を追加）：**11/11 成功**。
- [x] **カレンダーToDo表示をスクショ確認**：期日付きToDoが ✅ で該当日に表示（締切⏰・イベント📌と区別）。
- [x] **カンバン11列をスクショ確認**：インターン3段階を含む11列が横スクロールなしで1画面に収まる（列数連動）。

## 指摘事項
| # | 重大度 | 箇所 | 内容 | 対応 |
|---|--------|------|------|------|
| 14 | 中→済 | backend(運用) | tsx watch の連続再起動でポート競合（EADDRINUSE）・プロセスのゾンビ化 | 関連プロセスを一掃しクリーン再起動で解消。コードの問題ではなく開発運用上の注意 |
| 15 | 低 | frontend | カンバン11列を狭幅PCで詰めるとカード名の折り返しが増える | 横スクロール解消（要望16）を優先。許容 |

## 良い点
- カンバン列数を `STATUSES.length` 連動にしたことで、今後ステータスを増減してもレイアウトが自動追従（保守性向上）。
- `GET /api/tasks` を `/api/events` と同型で追加し、カレンダーの3種（イベント/締切/ToDo）を統一的に集約。

## 結論
**合格。** 重大度「高」なし。#14は是正済み、#15は許容。PMの最終承認へ。
