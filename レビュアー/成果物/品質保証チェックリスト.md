# 品質保証チェックリスト（就活コンパス）

レビュアー ／ 2026-06-12

| 観点 | 項目 | 結果 |
|------|------|------|
| 制約 | 有料サービス・有料APIを使っていない | ✅ |
| 制約 | 外部へのデータ送信がない（ローカル完結） | ✅ |
| 制約 | `npm install`→起動コマンドで動く構成になっている | ✅ |
| 動作 | バックエンドが起動しAPIが応答する設計 | ✅（要 npm install 後の起動確認） |
| 動作 | フロントが起動し主要画面が表示される設計 | ✅（要 npm install 後の起動確認） |
| 機能 | 企業のCRUDが可能 | ✅ |
| 機能 | イベントの追加・完了・削除が可能 | ✅ |
| 機能 | ダッシュボードで締切・状況が見える | ✅ |
| バグ | 異常系（404/400）を返す | ✅ |
| 入力検証 | 必須・enum・日付形式のバリデーション | ✅ |
| 可読性 | 命名・責務分割・コメント | ✅ |
| 一貫性 | TypeScript strict / 規約遵守 | ✅ |

## 備考
- 実機での `npm install` → 起動の動作確認は、ユーザー環境で実施を推奨（手順は各成果物の仕様書・ルートREADME参照）。
- better-sqlite3 のネイティブビルドに失敗する場合は、Node.jsのバージョン（18以上推奨）とビルド環境を確認。
