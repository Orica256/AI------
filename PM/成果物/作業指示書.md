# 作業指示書

作成日：2026-06-12 ／ 作成者：PM

## バックエンドエンジニアへ
- [ ] `成果物/` に Express + better-sqlite3 + TypeScript でAPIを構築。
- [ ] 要件定義のデータモデル・API仕様を実装。
- [ ] DBは `成果物/data/shukatsu.db`（自動生成）。初期スキーマ・シードを用意。
- [ ] CORSを許可（localhost:5173）。
- [ ] 入力バリデーションと異常系（404/400）を実装。
- [ ] `成果物/API仕様書.md` と起動手順を作成。
- **完了条件**：`npm run dev` でlocalhost:3001起動、全エンドポイントがcurlで応答。

## フロントエンドエンジニアへ
- [ ] `成果物/` に React + TypeScript + Vite でUIを構築。
- [ ] ダッシュボード／企業一覧／企業詳細・編集の3画面。
- [ ] APIと連携（ベースURLは定数管理、Viteプロキシで /api を3001へ）。
- [ ] ステータスのフィルタ、企業のCRUD、イベント追加・完了切替。
- [ ] `成果物/画面仕様書.md` と起動手順を作成。
- **完了条件**：`npm run dev` でlocalhost:5173起動、企業の登録〜削除が画面から可能。

## レビュアーへ
- [ ] 両エンジニアの完了後、コードレビューを実施。
- [ ] 制約遵守（無料・外部送信なし）を最優先で確認。
- [ ] `成果物/レビュー報告書.md`・`成果物/品質保証チェックリスト.md` を作成。
- **完了条件**：指摘の重大度判定と、合否所見をPMへ提出。

## 秘書へ
- [x] ユーザー要望の整理（完了）。
- [ ] 以降のユーザー意見も随時整理しPMへ共有。

## 全体の完了条件（PM最終チェック）
1. 制約（無料・ローカル）を全員が守っている。
2. アプリが起動し主要機能が動く。
3. レビュー合格。

---

# 第3サイクル 作業指示（2026-06-12）

## 背景・方針
実起動テストで、前サイクルの記録（カンバン/カレンダー/ToDo/エクスポート「実装済み」）と**実コードの乖離**が判明。本サイクルは「記録と実態の一致」を最優先に、秘書要望7の順序 **③→①→②** で進める。各成果物は「動作・配線・疎通を確認してから完了とする」。

## ③ ドキュメント整合（主担当：PM）
- [ ] `README.md`・`共有事項.md` を**実態どおり**に修正（実装済み／未実装を正確に）。
- **完了条件**：誇張・先取り記載がなく、現状の機能一覧が実コードと一致。

## ① 未配線コンポーネントの配線（主担当：フロント、補助：バック）
- [ ] `KanbanBoard` / `CalendarView` / `DeadlineBadge` を `App.tsx` にタブ統合し、画面から到達可能にする。
- [ ] 依存の欠落を解消：`types.ts` に `PRIORITY_LABELS`、`api.ts` に `listAllEvents`、**バックに `GET /api/events`（全イベント＋企業名）** を追加。
- [ ] スタイル（kanban/calendar/deadline-badge）を `styles.css` に追加。
- **完了条件**：5タブ（ダッシュボード/一覧/カンバン/カレンダー/詳細）が表示・遷移でき、`tsc` エラー0。

## ② ToDo・エクスポート/インポート（主担当：バック＋フロント）
- [ ] バック：`tasks` テーブル＋CRUD（`/api/companies/:id/tasks`, `/api/tasks/:id`）、`GET /api/export`・`POST /api/import`。
- [ ] フロント：`api.ts`・`types.ts` 反映、`CompanyDetail` にToDo欄、ヘッダにデータ入出力ボタン。
- **完了条件**：ToDoの追加/完了/削除、JSON書き出し/読み込みが画面から動作。`API仕様書.md`・`画面仕様書.md` を更新。

## レビュアーへ（重点）
- [ ] **未配線コンポーネント・未実装API・ドキュメントと実装の乖離**を必ず検出対象に含める。
- [ ] 「実装済み」記載は、起動・配線・疎通の確認が取れてから合格にする。

## 成長（CLAUDE.md への責務追記）
- [ ] 本サイクルで露呈した責務の穴（記録と実態の整合確認、未配線の禁止、API追加時のフロント反映）を、**該当アクターの `CLAUDE.md`** に追記する（指示9）。

---

# 第4サイクル 作業指示（2026-06-13）

## 背景・方針
ユーザー指示10により、使い勝手向上（A1〜A5）と品質基盤（C1）を実装する。AIは既定どおり **Claude が実装**（Codex指定なし）。
すべて **無料・ローカル完結**で実現可能。バックエンドのスキーマ変更は不要（A1/A2はクライアント側で完結）。

## フロントエンドエンジニアへ
- [ ] **A1 検索ボックス**：企業一覧で企業名・業界の部分一致フィルタ（クライアント側）。
- [ ] **A2 並べ替え**：一覧を「締切が近い順／志望度が高い順／デフォルト」で切替（クライアント側、締切なしは末尾）。
- [ ] **A3 締切リマインド強調**：ダッシュボードの締切に `DeadlineBadge` を適用し、超過・間近（3日以内）の件数を見出しで強調。
- [ ] **A4 メモMarkdown表示**：メモ欄を簡易Markdown（見出し`#`／箇条書き`-`／太字`**`／改行）でレンダリング。無依存・軽量・XSS安全（Reactの自動エスケープ）。編集時はtextareaのまま。
- [ ] **A5 ダークモード**：ヘッダにトグル、CSS変数で全体テーマ切替、`localStorage` で保持。
- [ ] 変更後に `npx tsc --noEmit` でエラー0、`画面仕様書.md` を更新。
- **完了条件**：各機能が画面から操作でき、型チェック通過、画面仕様書が実態と一致。

## レビュアーへ
- [ ] **C1 スモークテスト**：バックエンドのCRUD疎通（health→作成→一覧→取得→更新→削除→dashboard）を確認するスクリプトを `scripts/` に整備。
- [ ] A1〜A5を「記録と実態の整合」観点でレビュー（実コードに存在し動くか）。`tsc --noEmit` の結果を報告書に明記。
- **完了条件**：スモークテスト手順が再現可能、レビュー所見をPMへ提出。

## 全体の完了条件（PM最終チェック）
1. 制約（無料・ローカル・外部送信なし）を遵守。
2. A1〜A5が画面から動作し、C1で疎通確認できる。
3. ドキュメント（画面仕様書）が実態と一致。レビュー合格。

---

# 第5サイクル 作業指示（2026-06-13）

## 背景・方針
ユーザーの実画面フィードバック（要望13〜16）に対応。AIは既定どおり **Claude が実装**。無料・ローカル完結を維持。
本サイクルは**スキーマ変更（tasks.due_date）を含む**ため、既存DBを壊さない冪等マイグレーションを必須とする。

## バックエンドエンジニアへ（改善1）
- [ ] `tasks` に `due_date TEXT`（NULL許容）を追加。`CREATE TABLE` と、**既存DB向けの冪等マイグレーション**（`PRAGMA table_info(tasks)` で無ければ `ALTER TABLE tasks ADD COLUMN due_date TEXT`）。
- [ ] `POST /api/companies/:id/tasks`・`PUT /api/tasks/:id` で `due_date`（任意・YYYY-MM-DD or null）を受け付け、`isDateStr` でバリデーション。
- [ ] `GET /api/export`・`POST /api/import` の tasks に `due_date` を含める。
- [ ] `API仕様書.md` を更新。
- **完了条件**：期日あり/なしの両方でtask作成・更新でき、既存DBでも起動時エラーが出ない。

## フロントエンドエンジニアへ（改善1〜4）
- [ ] **改善1**：`types.Task` に `due_date`、`api.addTask` に任意 `due_date`。`CompanyDetail` のToDo追加フォームに**任意の日付欄**、一覧表示に期日。
- [ ] **改善2**：`CompanyList` の検索を**ひらがな↔カタカナ正規化**して一致判定。
- [ ] **改善3**：並び順に「企業名 昇順／降順」を追加（`localeCompare('ja')`）。
- [ ] **改善4**：カンバンを**8列グリッド**（`repeat(8, minmax(0,1fr))`）化し横スクロールを解消。カンバン表示時は `main` の幅を拡張。
- [ ] `npx tsc --noEmit` エラー0、`画面仕様書.md` を更新。
- **完了条件**：4点が画面から確認でき、型チェック通過。

## レビュアーへ
- [ ] `scripts/smoke-test.ps1` に **tasks（due_date含む）の作成・取得・削除**疎通を追加。
- [ ] tsc（両方）・smokeの結果を報告書に明記。スキーマ変更の後方互換（既存DB起動）を確認。
- **完了条件**：smoke再現可能、レビュー所見をPMへ提出。

## 全体の完了条件（PM最終チェック）
1. 制約（無料・ローカル・外部送信なし）を遵守。
2. 改善1〜4が動作し、smokeで疎通確認。既存DBが壊れない。
3. ドキュメント一致・レビュー合格。
