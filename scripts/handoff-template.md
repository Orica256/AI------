# Codex 作業指示テンプレ（ハンドオフ）

> 使い方：このファイルをコピーして1タスク=1ファイルにし、`scripts\codex-task.ps1 <そのファイル>` で委譲する。
> 規約ヘッダ（無料/ローカル・命名規則・型/API契約は変更不可 等）はラッパーが自動で付与するので、ここには**今回固有の内容だけ**書く。
> 「正」の型 / API契約は Claude が確定してここに貼る。Codex は中身を埋めるだけ。

---

## タイトル
（例：企業一覧に業界フィルタを追加）

## 背景 / 目的
（なぜやるか。1〜3行）

## 対象ファイル
- 触ってよい：`app/frontend/src/...`, `app/backend/src/...`
- 触らない：（型定義 / API契約など変更不可のもの）

## 確定済み API契約 / 型（変更不可・Claudeが管理）
```ts
// ここに「正」の型・エンドポイント定義を貼る。Codex はこれに従う。
// 例：
// GET /api/companies?status=&industry=  -> Company[]（既存にindustry絞込を追加）
```

## 作業内容（具体的に）
1.
2.
3.

## 完了条件（DoD）
- [ ] 指定の挙動を満たす
- [ ] `app/backend` または `app/frontend` で `npx tsc --noEmit` が通る
- [ ] 既存機能を壊していない（手元で起動確認 or 該当画面の動作）
- [ ] 命名規則・1ファイル1責務を守っている

## 制約 / 注意
- 依存パッケージの追加は原則しない（必要なら理由を出力して止まる）。
- DBスキーマ変更が要る場合は、マイグレーション方針を出力して確認を仰ぐ（無断で破壊的変更をしない）。
- コミットはしない（Claude が author=Orica256 で行う）。
